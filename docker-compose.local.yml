
services:
  cdn:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "${SERVER_PORT}:${SERVER_PORT}"
    environment:
      STORAGE_TYPE: "local"
      SERVER_PORT: "${SERVER_PORT}"
    volumes:
      - "${LOCAL_STORAGE_PATH}:/data"
